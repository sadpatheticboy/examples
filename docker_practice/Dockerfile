# Используем официальный образ Python c Docker Hub
FROM python:3.11

# Устанавливаем рабочую директорию в контейнере
WORKDIR /

# Копируем файлы из текущей директории в рабочую директорию в контейнере
COPY . .

# Запускаем скрипт
CMD ["python", "./timer.py"]
